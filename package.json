{
  "name": "dwg-viewer-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp -r libs images iconfont index.html draggable-measurement-extension.js dist/ 2>/dev/null || true"
  },
  "dependencies": {}
}
